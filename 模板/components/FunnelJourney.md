# FunnelJourney

## 任务

解释转化、注意力流失或逐层筛选过程。

## Modes

`conversion` / `attention` / `filter`

## 输入边界

- 3-6 个阶段，从宽到窄或从输入到结果。
- 阶段之间必须存在真实筛选或流失关系。
- 没有真实比例时使用阶段结构，不显示伪造百分比。

## 适用

点击到转化、停留到互动、候选筛选和内容传播链路。

## 禁用

普通流程没有逐层减少关系时使用 FlowNodeGraph。

## 质检

漏斗阶段顺序正确；右侧解释补足每层任务；不能只画形状而不解释为什么流失。
