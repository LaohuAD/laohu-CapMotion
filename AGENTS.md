# 老胡口播视频画面讲解项目操作手册

本项目用于沉淀“自由口述 → 教学视频成片”的长期后期体系。目标不是把素材塞满时间轴，而是形成可反复调用、可追溯、可人工返工，并且真正改善观看体验的 Cap + Remotion 生产线。

## 根本目的与四层架构

第一遍自由录制属于“老胡文稿”，只复用本项目 Cap→ASR 入口；第二遍正式录制完成后，本项目才在该视频专属任务中负责 EDL、字幕、Remotion、数字人、外部素材和 Cap 非破坏总装。

> 一条视频，只带观众走一条路。

每条作品先确定最终观众、观看承诺、主线和最该赢的一点，再处理剪口、动画与字幕。画面不是为了持续有变化，而是帮助观众理解关系、相信证据、看清操作、感受重点或顺利进入下一段。

> 声音定路，画面作证。

四层是每个项目 Skill 内部都要具备的能力，不是四个固定标题：

- 灵魂以箴言定向：解决观众变化、主胜负手、价值冲突和取舍。原则必须改变候选胜负，不能只写“追求高质量”。
- 筋骨以章法成篇：解决输入、状态、顺序、时间轴、段落承接、跨 Skill 交接、失败返回和最终回收。
- 血肉以技法生变：解决真实材料怎样选择，镜头、剪辑、动画、字幕或数字人作用于什么，为什么有效，怎样开始、变化和收束，过量会损害什么。
- 表皮以法度成形：硬规格精确检查；节奏、焦点、气质、信息密度和自然程度通过观看、候选对照与语境判断。

四层必须翻译成领域语言。只在 Skill 末尾贴一段“四层说明”，却不改变输入、中间结果、选择、否决和验收，不算能力建设。

权威关系：

```text
最终制作脚本 = 内容语义与公开承诺的权威
原始录制与源媒体 = 事实、动作和追溯的权威
原始词级 ASR = 源时间与原声定位证据，不是自动正确的文字
冻结 EDL + S2→T2 映射 = 成片时间的唯一权威
最终主音频 = 字幕、数字人与口型的声音权威
截图、数据、原文件与出处 = 事实画面的证据权威
PPT / 参考图 = 视觉参考，不能越级改写事实和内容
```

> 能播出只是底线，值得看完才算成片。

底线门阻止路径、时码、事实、接口、轨道和文件不可用；巅峰门还要检查主线是否清楚、关键材料是否进入、画面是否真正改变理解、最强段落是否被做到最好，以及是否仍有明确可执行的重要增益。漂亮特效不能补偿内容断裂，规则齐全也不能证明作品已经优秀。

## 本地应用开发与界面占用规则

当用户正在使用电脑时，允许后台编译、自动测试和热更新。用户进入测试阶段后，首次启动或偶尔一次原生重启把 Cap 带到前台可以接受；真正禁止的是连续、反复抢占焦点，导致用户无法稳定使用其他软件。

```text
触发条件：开发或验证 Cap 等本地 GUI 应用。
正确做法：前端 HMR 可以在已经打开但处于后台的 Cap 窗口中更新；一般情况下，用户切换到其他软件后，Cap 应继续留在后台。
原生改动：Rust / Tauri 改动优先后台编译和自动测试；确需重启验证时可以偶尔激活一次，但不能形成保存一次、抢一次窗口的连续循环。
启动条件：只有用户明确说“打开让我测试”或同义指令时，才首次启动可见应用；用户允许测试后，可以让应用保持打开并继续热更新。
并行开发：用户测试期间可以继续修改代码和热更新，不要求冻结开发；应控制原生重启频率，让用户能够连续完成其他操作。
人工验收：需要用户查看新结果时，由用户主动切回 Cap，或在用户明确要求时提示一次。
禁止做法：开发过程中短时间内反复弹出或激活 Cap，使用户刚切换到其他软件就被再次打断；把高频原生进程重启当作普通前端 HMR 使用。
```

本项目的人工测试以本机独立版 `/Users/a1/Applications/CapMotion.app` 为稳定入口。开发阶段可在源码开发版中后台编译和自动验证，但源码改动不会自动进入独立版；只有用户明确要求“重新打包”或同义指令时，才调用 `scripts/build-cap-local.sh` 更新独立版。该本地包固定使用名称 `CapMotion`、应用标识 `com.laohu.capmotion`、定位语 `Screen recording built for AI-driven timeline editing.`、Tauri 界面和独立数据目录；版本号以 `apps/desktop/src-tauri/tauri.local.conf.json` 为唯一权威，每次真正交付给用户安装的功能更新递增 `0.0.1`，个位进位按 `0.1.9 → 0.2.0`、`0.9.9 → 1.0.0` 处理。同一更新的失败重试、重新编译和重新签名仍沿用原版本，不重复加号。每次版本递增必须先在 `apps/desktop/src/capmotion-changelog.json` 顶部写全中英双语更新记录；打包脚本必须在昂贵编译前校验包版本与最新记录一致。该记录是本地未发布阶段与应用内更新日志的权威，发布 GitHub Release 时复用同一条记录，禁止继续显示或发布上游 Cap 的更新日志。本地及免费分发包必须使用同一张持久的 `CapMotion Local Code Signing` 证书签名，禁止使用 ad-hoc 签名或为后续构建重新生成证书；否则 macOS 会把更新后的二进制当成新应用，屏幕录制与辅助功能授权不能继承。打包脚本在证书缺失、指定要求含 `cdhash` 或两个非 ad-hoc 版本的要求不相容时必须拒绝更新。从旧 ad-hoc 版迁移到持久证书版时允许且仅需一次重新授权；此后覆盖更新必须通过权限继承回归。当前不做 Apple 公证，不生成官方更新产物，不打入 GPUI；如要普通用户无 Gatekeeper 提示地直接安装，必须升级为 Apple Developer ID 签名与公证，不能用自签证书冒充官方分发。首次安装只迁移一次旧开发版设置与录制工程，录制媒体使用同盘移动而非复制，避免额外占用一套空间。更新只检查 `LaohuAD/laohu-Voice2MotionFrameCut` 的 GitHub Releases，第一阶段只提示并打开下载页，不调用官方 Cap 更新或应用内安装。打包和替换时不得自动关闭用户正在测试的 Cap；若应用仍在运行，应先等待用户结束测试。

开发验收与发布打包必须分层。源码、Cap 工程交互、预设或本地编辑流程发生变更时，先在 `pnpm --dir apps/desktop dev:tauri` 启动的开发版中，用新建工作工程完整走通真实链路：文件打开、EDL 写入、源字幕导入、中英双轨物化、画幅与背景、预设保存、关闭重开和必要的导出或预览。已剪工程不得再作为源时间轴重建 EDL；源素材段边界始终从未剪原工程读取，待编辑工程只提供当前 revision。开发版中发现的问题继续在同一开发闭环修复并回归，不因每一个问题重新打包。只有全链路验收通过、已知问题关闭且用户已授权打包时，才退出开发版并单次更新独立版；独立版直接打开同一份已验证 `.cap` 工程做发布回归，不重建工程。若问题只在签名、权限、包标识或安装路径下出现，才把打包本身作为独立诊断变量。

## 本地化的技术文本边界

当 Cap 切换为中文时，只翻译面向用户理解和操作的显示文本，不能改写会参与程序运行或需要复制使用的技术内容。

```text
应翻译：窗口标题、按钮、标签、说明、提示、错误信息、交互状态和帮助文字。
保持英文：CLI 命令、代码语法、变量名、占位符名称、配置键、文件路径、URL、协议值、枚举值和品牌名。
混合内容：翻译外围解释，但代码片段原样保留。
示例：“点击占位符即可复制，时间支持 {moment:HH:mm} 自定义格式”；其中解释是中文，{moment:HH:mm} 不变。
禁止做法：为了界面看起来全中文而翻译 {recording_mode}、studio、S3、CLI 等会影响运行、复制或技术含义的内容。
```

## Agent 操作 Cap 工程的边界

人类编辑器与 Agent 接口必须操作同一个可返工的 Cap 工程模型。Agent 默认通过 Cap 已编译的 CLI / MCP 修改作品层数据，不直接改源码、schema 或渲染器，也不把整份 `project-config.json` 替换当作普通操作。写操作统一使用工程锁、`expectedRevision`、字段校验、原子写入和变更回执。完整接口契约见 `workflows/laohu-video/规范/Cap Agent 工程接口.md`。

公共能力只定义可配置字段、取值范围和渲染行为，不嵌入任何个人的字体、间距、阴影、布局等数值。个人值保存在操作系统用户所属的命名预设中；同一可见预设可另带一份可选 `agentProfile`，用于剪辑、字幕和 Remotion 的稳定制作偏好。应用预设到工程时只合并白名单展示参数，`agentProfile` 不直接写入 `.cap` 时间轴，也不携带或覆盖字幕正文、素材路径、音轨编辑和 Remotion 覆盖。

界面保存命名预设时，以当前编辑会话打开工程或最后一次应用／保存后的可复用配置为基线，只把基线之后真正改动的字段合并进预设最新值。打开预设菜单不得隐式应用预设，保存也不得要求先应用。未改字段、`agentProfile`、字幕正文、素材路径、时间轴、音轨编辑、Remotion 数据和 revision 必须保持原值。

字幕位置属于字幕轨而不是单条字幕；同轨字幕统一使用一个位置，中英轨可各自设置。手动位置以画面中心为 `0,0`，输入坐标、画布拖动和方向键必须同步到同一轨级值。字幕动画只做入场：“无动画”不得带任何淡入淡出或位移，渐显只改透明度，上移显现只改入场位移，弹入只改入场缩放，字幕结束时不延长存活时间做出场动画。

Cap 的“工程与媒体存储位置”是唯一的新建大文件根路径：新录制工程、截图工程和工程内导入图片都跟随它；未自定义时仍使用 Application Support。已有工程不强制搬迁且必须继续可见，只有用户显式选择迁移时才移动。工程使用的导入背景必须存入该 `.cap/assets/`，不得再把新 `bg-*` 文件写到 Application Support 根目录。

## Git 分支与本地工作目录规则

本项目采用单窗口、单工作目录的串行开发方式。默认只在当前项目目录中使用普通 Git 分支管理，不创建额外 Git worktree。

```text
触发条件：开发新功能、修复问题、创建功能分支、合并主分支或准备并行方案。
正确做法：在当前项目目录中创建或切换普通 Git 分支；完成验证后合并回 main，再删除已完成的本地功能分支。
切换前检查：切换分支前先检查未提交改动；需要保留时优先提交，不能擅自覆盖、清理或 stash 用户改动。
空间原因：额外工作树会增加一套工作文件，并可能重复生成 node_modules、target、构建缓存和临时产物，不符合本项目的磁盘占用要求。
例外条件：只有用户当轮明确要求创建 worktree 时才允许使用，并应事先说明预计增加的目录和磁盘占用。
禁止做法：因工具或 skill 的默认建议自行创建 `.worktrees/`、并行工作树或为同一项目复制多套依赖与构建缓存。
```

## 新对话与任务路由

进入本目录先读 `AGENTS.md`，再按真实任务选择一个主 Skill。不要一开始把全部视觉规范、经验库和技术资料塞进上下文。

| 真实任务 | 唯一主入口 | 必要边界 |
| --- | --- | --- |
| 第一遍 Cap 自由录制转文字 | `.agents/skills/volcengine-asr-srt/SKILL.md` + `scripts/cap-project-asr.mjs` | 由上游原任务直调，不创建后期任务 |
| 第二遍正式视频后期总装 | `.agents/skills/laohu-video-postproduction/SKILL.md` | 冻结 EDL 后才生成覆盖资产 |
| 程序动画设计与实现 | `.agents/skills/laohu-animation-director/SKILL.md` | 先转译粗建议；正式制作冻结 T2，唯一引擎 Remotion |
| RunningHub 数字人 | `.agents/skills/runninghub-avatar/SKILL.md` | 只用冻结后的最终主音频 |
| 修正 SRT | `.agents/skills/correct-srt-subtitles/SKILL.md` | 校对不等于烧录，不借机改写观点 |
| 字幕烧录 | `.agents/skills/burn-subtitles/SKILL.md` | 只接已确认校对稿和词级时间 |
| 用户明确授权剪映识别 | `.agents/skills/jianying-srt-bridge/SKILL.md` | 不操作既有正式草稿 |
| 项目规则、Skill、调用链和长期能力进化 | `.agents/skills/laohu-video-evolution/SKILL.md` | 先找最早失效点，再改唯一权威 |

专业知识必须留下内部激活回执：为什么此刻需要、取得了什么判断、改变或确认了哪个决定、结果落在哪里、哪些内容本轮没有采用。只写“已阅读”“已参考”不算知识生效。机械格式转换可以免除，重要创作、视觉判断与规则进化不能免除。

## 目录说明

```text
workflows/laohu-video/规范/  视觉、画面表达、组件、流程、质检和提示词的唯一规则目录
workflows/laohu-video/模板/  Remotion workspace、组件、EDL 与字幕工具的唯一模板目录
知识沉淀/                    经验证的偏好、踩坑、视觉问题、动效问题和参考案例
作品/                        每一条具体视频作品，按编号独立建目录
参考项目/                    第三方参考项目，只用于学习流程、结构和方法，不默认复制代码
归档/                        已明确结束或废弃的历史材料；当前仍在制作的作品不要放入这里
```

根目录 `模板/` 只允许保留指向权威模板目录的路由说明，不复制组件、规则、依赖或 workspace。

## 新作品创建规则

每次开始一条新内容时，在 `作品/` 下新建编号目录：

```text
作品/
  001_超级音乐小白流量进阶指南/
  002_下一条口播主题/
  003_再下一条口播主题/
```

单条作品目录结构：

```text
作品/编号_主题/
  控制文档.md  当前作品给执行 AI 读取的总控指令，记录用户如何要求优化这条作品
  输入/        文字型输入与素材索引：口播稿、ASR/SRT、源媒体绝对路径、参考说明
  分析/        内容分析、分镜、画面规划、时间轴规划
  制作/        任务包、Remotion 配置、素材索引和文字型覆盖记录
  输出/        文字型交付记录：外部成片绝对路径、成片 ASR/SRT、质检报告、最终说明
```

`控制文档.md` 是单作品的有限自由度执行蓝图，只累加当前作品仍有效的决定，不保存已经失效的争论和项目级规则副本。后续 002、003 等作品也应保留这个文件，用于记录“这条作品为什么这样做”。

控制文档不是泛泛方向建议，而是给执行 AI 的制作蓝图：需要写清楚 T2 场景时间、主标题、副标题、必须出现的文字、图形关系、排版结构、动效节拍、Remotion / 数字人 / 录屏 / 证据职责、必做项和可调整项。执行 AI 可以在边界内优化，但不能随意重设方向。

## 反馈分流与受控进化

> 一次反馈照见问题，反复验证才立规矩。

用户反馈先修当前结果，再判断它属于当前作品决定、稳定个人偏好、待验证经验、项目能力缺口还是已经确认的事实修正。当前作品决定写入该作品的 `控制文档.md`；只有老胡明确要求沉淀、优化项目或升级规则时，才由 `laohu-video-evolution` 进入项目级修改。

项目进化必须形成：失败证据 → 最早失效点 → 四层根因 → 可证伪假设 → 保护项 → 唯一权威 → 当前、旧任务和相邻任务回归 → `KEEP / OBSERVE / REVERT`。新反馈与旧规则冲突时不能直接覆盖；先判断适用条件、反转条件和谁在什么场景拥有优先级。

写回位置：

- 共同目的、权限、路由和不可补偿边界：`AGENTS.md`。
- 某类任务的判断与执行：对应 `.agents/skills/*/SKILL.md`。
- 只在特定条件需要的知识：该 Skill 的 `references/` 或 `workflows/laohu-video/规范/`，并由 Skill 写明触发条件。
- 已验证用户偏好与失败经验：`知识沉淀/`。
- 可复用组件与实现：`workflows/laohu-video/模板/`。
- 行为保护：对应测试和 `test-prompts.json`。

不能因为文件变短、目录整齐或规则听起来有道理就删除旧能力。大幅改写前回到 Git 原文提取触发、动作、因果、例外和失败分支；先在新位置接通，再撤旧入口。

## 程序动画路由

虚拟成片的主审稿默认交付可视化 HTML：顶部疑难，中央成片轴，左侧成片时码/源时码/最终字幕或无字幕事件，右侧跨片段的动画方案及逐图用途。内容使用当前联合审稿模板的全部必要信息；按 `workflows/laohu-video/模板/口播精剪审稿模板.md` 维护唯一审稿数据并生成视图，不退回旧纯字幕表、不另维护竞争主稿。

包含画面增强的完整后期，动画策划必须纳入剪辑前同一份审稿主文档：对应讲述与组合片段、预期成片起止、画面效果、覆盖方式、图片/截图/文字用途与音效安排一起讨论。用户反馈直接更新对应单元及受影响时码；当前整份方案批准后，才执行剪辑、动画实现和媒体生成。局部同意或旧剪口批准不能代替当前整体批准；纯剪辑不自动增加动画，已授权局部返工不扩大到整片重审。具体流程、审稿修订与执行检查分别由后期 Skill、动画导演转译规范和后期任务包契约承接。

程序动画唯一使用 Remotion，成片总装使用 Cap Motion 上层轨。 整片/章节请求先由动画导演根据虚拟成片连续内容、词级映射和真实底画，判断动画必要性、片段组合、实际覆盖起止与素材需求；再把制作脚本的粗略建议转译成画面、动作、镜头、节拍、声音设计和结果，调用官方实现知识；设计可先于冻结，正式渲染与覆盖仍须冻结 T2。参考拆解提供有适用边界的方法，组件库提供可复用实现，不把任何博主的皮肤或现有家族设为全部动画的固定格式。执行动画时读取 `.agents/skills/laohu-animation-director/SKILL.md`；执行第二遍完整后期时读取 `.agents/skills/laohu-video-postproduction/SKILL.md`。不得创建或调用其他程序动画引擎入口。

## 口播视频精剪规则

当用户提供口播视频和 SRT / ASR，并要求“根据字幕剪辑视频”时，默认交付纯剪辑视频：

- 用户要求完整剪辑、粗剪接精剪或成片流水线时，必须先在文档中完整走一遍虚拟初剪、虚拟精剪、字幕切分和字幕顺句，不直接操作媒体。预剪辑字幕审稿文档必须覆盖整条源视频，按每个候选剪辑片段列出源时码、字幕原文、建议保留 / 删除 / 待确认及具体理由；核心交付还必须给出“如果按此方案剪完”的预期成片字幕时间轴，逐段列出目标时码和最终拟用字幕。生成这条时间轴前必须跨越相邻 ASR 条目恢复连续词流，检查上一条是否留下未完成句、下一条是否只是上一句的宾语或句尾，不能把 ASR 的机械分段直接交给用户。所有会影响保留范围、字幕文字、专名、字体或样式的疑难都在文档中集中询问。只有用户明确批准整份虚拟成片方案并确认疑难项后，才能冻结 EDL、执行媒体剪辑或渲染。未经批准的 EDL 和样片只能标为历史草案，不能作为继续制作的授权。
- 用户把“粗剪 -> 精剪 -> 修正SRT字幕”定义为一条成片流水线的三个连续阶段，不是三份并列交付物。默认只交付完成全部阶段后的最终成片和正式校对 SRT；粗剪视频、未校对精剪视频、修正草稿都属于内部中间件，统一放在任务临时目录并在下一阶段验证完成后删除。只有用户明确要求单独查看、比较或交付某个阶段时，才保留该阶段产物。
- 当用户只明确要求“粗剪”而没有要求完整流水线时，粗剪仍按独立任务边界执行；不能因为存在完整流水线规则就擅自进入精剪、字幕校对或烧录。
- 用户说“修正SRT字幕”时，固定调用项目级 `.agents/skills/correct-srt-subtitles/`：保留原始成片 SRT，逐条检查专名、英文、数字、专业术语、同音错词、上下文、断句和剪口；确定项直接进入修正草稿，不确定项保持原文并生成带时码的疑难清单，待用户确认后再生成正式校对 SRT。该触发词不自动授权字幕烧录，也不允许借校对继续改写观点或精剪内容。
- 用户说“字幕的烧录”时，固定调用项目级 `.agents/skills/burn-subtitles/`：只使用与当前成片对应并已确认的正式校对 SRT，横屏统一为 16:9，源画面完整保留并贴顶、黑色留白放在下方，按思源黑体加粗、白字黑描边和黑色阴影的固定样式烧录。默认只输出中英双语版；只有用户明确要求中文版时，才额外或单独输出中文单语版。首次采用或改变样式时先出一份双语短样片，用户确认后再渲染全片；样片时长以用户当轮要求为准，未指定时约 10 秒。
- 正式校对 SRT 是保真主稿，不得把其中十几秒的长条目原样烧录。字幕烧录前必须结合原始 JSON 的词级时间戳，跨越相邻 SRT 条目恢复连续词流，再按气口、完整意群和屏幕可读长度生成展示稿；时码取首尾保留词的真实时间，禁止按字符数平均分配。展示稿可去除无意义语气词、口吃和紧邻自我修正，也可把误落在上一条末尾的功能词或下一条开头的句尾移回完整意群；但完整语义重复应回到精剪处理音视频。`MiniMax H3`、`Seedance 2.5`、`Wan 3` 等模型名和版本号属于不可拆原子，宁可整组移到相邻字幕，也不能从中间断开。中文默认无停顿标点，英文也默认无句末句号和非必要逗号，长句优先继续拆条。

- 所有视频、音频、渲染成片等二进制媒体产物默认不保存在本项目目录。输入视频位于哪个目录，完成全部请求阶段后的最终媒体就输出到该输入视频所在目录；例如输入是 `/Users/a1/Movies/a.mp4`，完整剪辑流水线默认只交付 `/Users/a1/Movies/a_成片.mp4`。不得再把成片放入 `作品/.../输出/`。
- 项目内的 `输入/`、`分析/`、`输出/` 只保留可回溯的文字型记录，例如素材绝对路径索引、控制文档、ASR/SRT、原始识别 JSON、EDL、剪辑简报、质检报告和经验记录。不得复制原始视频、音频或长期保存粗剪、精剪、渲染视频等大文件。
- 剪辑过程中确有必要的音频、分片、测试视频和中间渲染统一放在 `/tmp` 或任务专用临时目录，交付验证完成后删除。项目记录必须写明最终媒体的绝对路径、时长、校验结果和删除依据，使媒体不在项目内也能回溯。
- 多个输入视频位于不同目录时，默认把最终合并成片输出到剪辑简报中列为“主输入”的视频所在目录；如果没有明确主输入，则使用用户指定顺序中的第一个视频目录，并在素材索引和交付报告中记录该选择。
- 输出前先检查目标路径是否已有同名文件。未经用户明确允许不得静默覆盖已有成片；发生冲突时先保留现有文件并说明。

- 用户要求“重新识别”或“跑完整流程”时，必须从当前指定的视频或音频发起一次新 ASR；即使用户同时提供了旧 SRT，也只能把旧 SRT 当作术语和漏句的交叉校对来源，不能用它替代新识别。
- 当前已验证的文字识别基准只有剪映 SRT。Apple Speech 已退出本项目工作流，不得用于主识别、置信度比较、交叉校对或疑难筛选。
- 正式无 GUI ASR 的技术默认是火山引擎“大模型录音文件识别极速版”HTTP API。新版 API Key 与现有试用包已于 2026-07-27 通过真实中文音频调用验证。本地音频不超过 2 小时且不超过 100MB 时优先用 Base64 `audio.data` 直传，不经 TOS；超过限制、已有受控 HTTPS URL 或用户明确优先低成本时，使用标准版 URL 模式。技术接入不等于准确率验收：仍必须用同一段人工校对样本与剪映比较专名、数字、英文、漏句和时间戳，不能因同属字节就假定同模型或同准确率。
- 火山引擎 ASR 的本机私密配置固定放在 `~/.config/laohu/volcengine-asr.env`，不进入项目和 Git。执行新 SRT 识别前先读取该文件；文件存在且变量完整时直接使用，不再向用户重复索要。禁止在命令输出、日志、分析文件或回复中打印凭据值。只有凭据失效、服务未开通或用户主动要求轮换时才说明需要重新配置。
- 当前默认只消耗火山引擎已有试用额度，不得为了 ASR 任务开通后付费或自动购买资源。试用额度不足、接口要求开通计费或预计超出试用范围时，停止调用并先向用户说明成本和选择。
- 火山引擎文件识别统一使用项目级 `.agents/skills/volcengine-asr-srt/`。用户说“重新识别 SRT、用火山识别、文件转字幕”时，先按该 skill 检查鉴权和媒体限制、抽取音频、选择极速版直传或标准版 URL、保留原始 JSON、生成并校验 SRT。不得临时重写调用脚本，也不得改用匿名文件上传站。
- 未配置并验收正式文件 ASR 时：用户提供剪映 SRT 就直接以它为主稿；用户要求新识别且当轮允许剪映 GUI 时，使用 `.agents/skills/jianying-srt-bridge/`；既没有 SRT 又不允许 GUI 时，应说明缺少已验证主 ASR，不能偷偷换用其他识别器。
- 精剪成片字幕校验采用风险分级：复杂重排、大量短剪口、音画同步异常或 EDL 映射冲突时，使用“成片重新识别 + 已确认 EDL 映射”交叉校验；简单且映射可靠的顺序剪辑，可直接使用 EDL 映射并重点抽查剪口。任何冲突都进入疑难清单，不得静默覆盖。
- 未经用户当轮明确允许，不得操作剪映、豆包输入法或其他 GUI。只有用户当轮明确指定“用剪映识别”时，才使用 `.agents/skills/jianying-srt-bridge/`，并严格保护用户正式草稿。
- “粗剪”是独立且边界明确的任务：默认只删除空镜、无讲话停顿、废弃起头、口误重说和语义重复，并完成必要的多视频拼接。除非用户明确要求动画、包装或画面增强，否则不得启动 Remotion 或其他动画制作流程。
- “精确剪辑”不同于粗剪，但内容导演不是强制重构。先判断原录制是否已经有清楚主线；主线成立时保持原顺序和个人表达，只删除明确的长气口、独立“嗯/啊”、失败起头、紧邻自我纠正和可证明的重复试说。只有用户明确要求重构，或存在补录错位、依据结论倒置等可证明的理解断裂时，才重排章节或压缩完整表达。
- 精确剪辑必须分两层判断：第一层是内容导演，决定章节、目标顺序、保留价值、重复关系和补录位置；第二层才是技术剪口，定位词句对应的音频与视频边界。没有先完成内容导演，不得直接按“嗯、啊、然后、比如说”等字面搜索批量删除。
- ASR 可能把 `老胡` 识别成 `老婆`、把 `GitHub` 识别成其他英文、把 `文戏 / 武戏`、`Agent / skill` 等术语识错。应结合上下文恢复最可能语义，用恢复后的语义判断价值；但不因字幕错字就删除原视频中的正确口播。
- 语气词只有在独立成段、没有逻辑作用、且音频边界可确认时才删除。`比如说` 如果正在引出案例、`但是` 如果承担转折、`所以` 如果承担结论，都不是口水话；禁止按词表机械删除。
- 精确剪辑的保留优先级依次是：核心方法与知识、案例和证据、适用边界与失败经验、体现真实投入和长期实践的可信度信息、必要的人格与情绪表达、操作同步提示。纯排期闲聊、重复销售表达、夸张但无证据的自我评价优先压缩。
- 章节可以重排，但连续软件操作、案例播放和依赖画面状态的演示内部不得随意打乱。补录内容可拆成多个片段，分别插回其对应主题，例如情绪补录进入情绪章节，AI 音乐补录进入 BGM 章节。
- 精确剪辑必须保存 EDL：每个目标片段记录目标章节、目标顺序、原始 SRT 条目、全局时间、源文件、局部时间、保留或删除理由。重排之后仍要能追溯到原始素材。
- 精确剪口不能只相信字幕起止时间。删除短语气词或半句时，应结合低音量区、相邻词边界和画面动作确认；无法可靠分离时，宁可保留自然口语，也不能截断正确词句或造成嘴型跳变。
- 不烧录字幕，不添加字幕流，除非用户明确要求。
- 用户明确要求烧录字幕时，必须先读取 `workflows/laohu-video/规范/字幕制作规则.md`，先映射并校对 SRT、提交疑难清单，待用户确认后再按固定样式烧录。不得把未确认的 ASR 错词直接固化到视频中。
- 字幕 / ASR 只作为时间轴和语义判断依据。
- `无字幕` 不等于 `无内容`。当视频包含作品播放、音乐、效果声、案例演示或用户明确要求保留有声展示时，字幕只负责定位讲话，音频活动本身也是内容。只有同时缺少有效声音、字幕语义和必要画面动作的区间，才能按空镜删除；禁止把 SRT 条目之间的空白直接整段裁掉。
- 初剪中的“气口 / 演示”必须按证据顺序判断：录制音频只分为麦克风声音和系统声音，用户所说的外部录音属于麦克风声音，不另立第三类。先检查麦克风是否有人物讲话，再检查系统声音是否持续有效；两者都没有有效声音的区间优先作为气口候选，系统声音连续存在时才继续结合画面变化与前后语境判断是否为作品播放或操作演示。仅凭时长较长、页面有变化或 ASR 空白，不能把区间命名为动画 / 演示；音频类型、持续性或画面用途任一证据冲突时，必须进入预剪辑疑难清单并由用户确认。时长只提供权重，不得作为单一硬阈值。
- 对作品播放或音乐演示区间，不能用短时静音阈值把连续作品切碎。应先识别并保护完整演示区间，保留音乐停顿、节奏空拍和效果声尾音；普通讲话的相邻剪口默认保留约 `0.1–0.2` 秒真实气口，避免把发声起音和句尾收音剪掉。
- 直播、访谈或远程连线存在多人音量差时，最终成片默认做整片响度校准：先用温和动态归一化拉近小声与大声讲话，再以约 `-16 LUFS`、`-1.5 dBTP`、`LRA 7` 作为网络视频的默认交付目标。必须在所有批次合并后统一处理，禁止每个分片分别归一化，否则分片之间会忽大忽小。包含音乐或作品播放时要限制动态增益，不能把音乐强弱和情绪起伏压平。
- 当多个源视频共用一条按拼接后总时长生成的 SRT / ASR 时，必须先按用户指定顺序排列源视频，使用每个源视频的精确时长计算累计偏移，再把全局字幕时间映射回对应源文件的局部时间。不得把同一条全局时间轴直接套到每个视频上，也不得在源文件分界处重复或遗漏内容。
- 多源视频的画幅、分辨率或显示比例不一致时，默认保持每个源画面的完整内容，统一到一个交付画布后再拼接；除非用户明确要求裁切，否则优先等比缩放加留边，不为铺满画面裁掉软件界面或操作区域。
- 剪辑判断和导出技术必须分开：先基于字幕建立“保留 / 删除 / 待确认”的审稿决策，再根据确认后的决策生成剪辑区间和导出文件。不要一边想语义一边直接硬剪，除非任务极小且风险很低。
- 完整流水线默认先完成虚拟剪辑、后统一执行媒体渲染：先建立粗剪 EDL，再在同一时间映射上完成内容精剪、重排和字幕文字决策；所有会改变保留区间、剪口或顺序的疑难项必须在渲染前确认。EDL 冻结后只渲染一次无字幕成片；随后处理只影响屏幕文字的疑难项，再烧录默认双语交付。不得为了每个内部阶段分别渲染并长期保留媒体。
- 粗剪只解决无有效声音的气口、空白和已经确认无意义的等待；精剪才判断失败起头、前一句录坏后紧邻重录、同一内容的重复表达以及其他语义冗余。重复删除默认保留后面更完整、更自然的一版，但必须先验证前一版没有独立信息点、限定条件或操作同步价值。
- 精剪 EDL 冻结后，先由保留语音及词级时码重新派生字幕，不沿用被删除片段的旧条目。展示字幕按完整意群、自然气口和屏幕可读长度重新切分；在不改变事实、观点、语气强弱和口播含义的前提下，可调整口语冗余与文字语序，使书面阅读更通顺。每次切分后反向检查：字幕不得以无承接的“有、的、和、然后”收尾，不得把“完 / 整”“构 / 图”或完整模型名拆开，也不得留下只有结合上一条 ASR 才能理解的残句。上下文只有一个合理答案的术语错词直接修正并记入修正记录；存在两个以上合理候选时才进入疑难清单。除已确认的系统声音演示外，每段有效人物讲话都必须被字幕连续覆盖；音频存在而字幕缺失属于失败。最终沿用用户已确认的中英双语、字体与样式，样式有变化时仍需先确认。
- 疑难项分两类处理：会改变音视频保留范围、剪口或章节顺序的属于“剪辑疑难”，必须在无字幕成片渲染前确认；只影响错别字、断句、字幕清理或英文翻译的属于“文字疑难”，可在无字幕成片验证后、字幕烧录前确认。
- 无字幕成片完成后的复核采用风险触发：复杂重排、大量短剪口、音画同步异常或 EDL 映射冲突时，必须对成片完整重新 ASR；简单且映射可靠的顺序剪辑，可使用 EDL 映射加剪口重点抽查。不得把“每次都完整重识别”当作没有收益的固定消耗，也不得在高风险成片上省略验证。
- 开剪前先写清本条视频的剪辑简报：目标时长、观众、输出风格、必须保留、可以删除、主线判断、4-7 个顺序故事节点。没有这个简报，很容易只追求短和平滑，最后把背景、转折或句尾删没。
- 对每条删除都要能说明理由：重复试说、语义废弃、长停顿、弱过渡、离题支线、口误重说、信息已经落地等。不能因为“看起来可以删”就删。
- 第一优先级是内容完整度，第二优先级才是压缩冗余。剪辑前必须先确认每个独立信息点、论据、转折、情绪表达和完整句尾是否至少保留一版，再去删除重复、停顿和废弃试说。
- 不要只按大段区间粗剪；大段内部的思考、组织语言、停顿也必须继续切掉。
- 默认只删除波形确认的静音中段，相邻讲话片段合计保留约 `0.1–0.2` 秒真实气口；不能直接拿 ASR 词级起止当剪口，因为识别时码可能晚于辅音起音或早于句尾收音。超过约 `0.3` 秒的空白再结合语义、声音类型和画面动作判断删除或保护。
- 删除重复时要区分“重复废弃”和“独立语义”：同一意思多次试说时保留更顺的一版，但背景、主办方、事件名称、关键论据、转折和结论等独立信息点至少保留一版。
- 重复删除不只看静音：有声音但明显是废弃试说、重复起头、重新组织语言、前半句说错后后半句重说，也要删除。
- 不能把所有字面重复都当成废弃试说。教程中为了配合连续屏幕操作而有意说出的“再删掉”“再选择一个”等动作提示，仍然承担时间同步和操作解释作用；只有确认属于录坏后的重录版本，才按语义重复删除。
- 不允许为了追求短和平滑，把只出现一次的完整句尾、情绪补充、限定条件或解释性短句剪掉。像“强烈的情绪价值”这类承接上一句的句尾，必须跟前一句作为一个完整表达保留。
- 剪辑导出应尽量保留源视频音画同步：优先从原始视频按保留区间重新切片并拼接，不要只重建音频或只处理视频。若使用复杂滤镜或 concat，导出后必须检查音频流和视频流起点是否都从 0 开始。
- 当一条视频包含大量短保留区间时，不要只用一条 `select / aselect` 表达式连续丢帧后分别重建时间戳；视频帧和音频帧的取整误差会在同一批次内累积，可能造成逐步音画偏移。应对每个保留区间分别使用成对的 `trim / atrim` 和 `setpts / asetpts`，再用 concat 把每一对音画区间拼接；交付前检查各批次音视频时长差，保守目标不超过一个视频帧加一个音频帧。
- 最终交付前必须用 `ffprobe` 确认只有视频流和音频流；还要用 `silencedetect` 做静音复查。检测到超过 `0.2` 秒的静音时必须逐项分类，但不能机械收紧：自然气口、情绪停顿、作品节拍和必要画面等待可以保护，只有确认无语义、无有效声音且无必要画面动作的中段才删除。
- 如果出现 `Non-monotonic DTS`、`Queue input is backward in time` 这类 ffmpeg 时间戳警告，不能只凭警告判断失败或成功；必须继续做 demux / 播放 / 抽帧 / 音画同步验证。验证失败时再用更精确的重编码或重新生成切片方案。
- 视频剪辑和渲染任务必须控制磁盘占用：中间 WAV、分片、临时 chunk、测试输出和失败输出用完即删；当前可用媒体成品保存在主输入媒体所在目录，项目内只保留必要的文字型分析和追溯记录。
- 大视频、渲染输出和临时产物不要进入 Git 跟踪范围，也不要触发 Git 打包。若磁盘突然减少，应优先检查 `.git/objects/pack/tmp_pack_*`、`.git/objects/**/tmp_obj_*` 和 `git count-objects -vH` 的 `garbage / size-garbage`；确认无 Git 进程占用后清理这些 Git 临时垃圾。
- 每次长视频处理结束前都要复查 `df -h` 和当前作品的大文件列表，避免把临时文件、失败导出或 Git 垃圾留给下一轮任务。

## 当前第一条作品

当前第一条作品目录：

```text
作品/001_超级音乐小白流量进阶指南/
```

当前制作基础位于：

```text
作品/001_超级音乐小白流量进阶指南/制作/
```

当前第一版的分析、待改效果和经验沉淀位于：

```text
作品/001_超级音乐小白流量进阶指南/分析/
```

注意：`作品/001_超级音乐小白流量进阶指南/` 仍在制作中，不属于归档项目。后续沟通时应在这条作品基础上继续修改效果；只有明确授权项目级沉淀时，才按受控进化流程写入 `知识沉淀/` 或 `workflows/laohu-video/规范/`。
