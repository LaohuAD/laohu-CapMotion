# 后期审计与剪辑简报

## 剪辑目标

- 观众：第一次使用工作流的普通用户，以及需要理解 Sigma / Latent 关系的 ComfyUI 用户。
- 目标时长：10—14 分钟；当前冻结时间轴约 13:22。
- 风格：屏幕证据优先、口播紧凑、关系动画只在解释困难处出现。
- 主线：输入与基础参数 → 一采/二采分工 → Latent 传递 → 模型按任务选择 → 三点回收。

## 剪辑判断

原始录制约 32:47。本轮没有按“无字幕就删除”处理，而是联合词级 ASR、声音活动、画面证据和脚本职责建立 EDL。共形成 195 个顺序片段，明确删除 20 个失败起头、立即纠正或重复试说，并保护 10 个真实操作/声音证据区间。完整决定与理由见 `制作/final.edl.json`。

S01—S12 均已建立语义映射，唯一精确时码以 `制作/source-to-final.json` 为准。S08 的真实结果证据与 S09/S10 的模型、穿模讨论在源录制中存在交叉，不能仅凭一个章节外包时间段认定互斥；调用方必须使用 JSON 中的逐片段映射。

## Remotion 节拍

| 成片时间 | 片段 | 作用 | 收束 |
| --- | --- | --- | --- |
| 142.22—150.22 | 比例锁定 | 证明一采和二采画幅必须一致 | 两端以锁链归一 |
| 230—242 | 同一采样链 | 解释一采中途停、二采继续 | 结构与细节落在同一轨道 |
| 273.5—285.5 | Latent 传递 | 点亮降噪输出、潜空间放大和二采关系 | 汇入最终视频 |
| 673.2—685.2 | 按任务选模型 | 区分自行生声和外部音频的取舍 | 不做单一画质排名 |
| 706.6—713.6 | 三点回收 | 收束结构、传递和模型选择 | 接结尾口播 |

证据缺口使用三个可替换占位：0—4.4 秒开头结果、408.3—413.3 秒同任务 A/B、600.33—606.33 秒三档强度对比。占位明确标注“待补”，不能作为发布证据。

## 字幕策略

- 源字幕 233 条，成片字幕 214 条。
- 只修正确定项：MiniMax、Sigma、Latent、LoRA、一采/二采、双时钟等。
- 不确定模型名、产品名、节点名和参数没有凭猜测替换。
- 字幕轨跟随冻结 T2 时间轴，源 SRT 保留用于追溯。

## REVIEW_REQUIRED

以下内容不能以当前基础成片直接作为确定事实发布：

1. `C012` 的准确模型身份与替代关系。
2. 运行时间以及 832 的真实含义。
3. `0.5—0.6`、`0.9—1.2` 的单位和节点职责。
4. 二采 4 步还是 5 步、Sigma 起止与 `0.49231` 的含义。
5. Lite / Plus 的显存与稳定结论。
6. FL、REF、真实人像模型、加速 LoRA、注意力与双时钟节点的准确名称。
7. 双时钟与中间音画不同步的因果表述，目前只能作为本次观察，不能扩大为普遍机制。
8. 锤子穿模与提示词空间关系的因果，只能限定为本次素材的合理分析。
9. 结尾中识别不清的模型比较名称。

## 回退点

- 原始 Cap 工程 revision 1 是总回退点。
- EDL 写入前后 revision 由回执记录。
- Motion、证据占位和字幕轨均可按独立上层轨移除，不需要重写原始媒体。
- 任何新的事实证据进入后，只替换占位或修订对应结论，不重跑无关 ASR 和底层剪辑。
